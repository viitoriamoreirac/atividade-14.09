distancia = float(input("Digite a distancia a percorrer (em quilometros): "))

velocidade_media = float(input("Digite a velocidade media esperada (em km/h): "))

tempo = distancia / velocidade_media

print(f"O tempo estimado da viagem é de {tempo:.2f} horas.")
